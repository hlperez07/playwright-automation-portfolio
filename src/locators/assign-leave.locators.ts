export const AssignLeaveLocators = {
  headingName: 'Assign Leave',
  /** Text of the oxd-form-row that contains the Leave Type dropdown */
  leaveTypeRowText: 'Leave Type',
  /** Date input placeholder on both Assign Leave date fields.
   *  OrangeHRM shows 'yyyy-dd-mm' (placeholder typo) but accepts ISO yyyy-mm-dd format. */
  datePlaceholder: 'yyyy-dd-mm',
  assignButton: 'Assign',
} as const;
